import React from "react";

const Button = () => {
  return (
    <button className="font-bold px-10 py-2 rounded-full cursor-pointer text-white bg-primary-400">
      Join
    </button>
  );
};

export default Button;
 